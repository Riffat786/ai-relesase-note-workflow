{
  "mcpServers": {
    "github": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_TOKEN": "$GITHUB_TOKEN"
      },
      "description": "GitHub MCP — provides access to GitHub API for reading issues, labels, and milestones. Requires GITHUB_TOKEN environment variable. Repository: ShubhKN/WealthWise"
    }
  }
}
